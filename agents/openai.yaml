interface:
  display_name: "jsonspecs Rule Author"
  short_description: "Author production jsonspecs rule packages"
  default_prompt: "Use $jsonspecs-rule-author to design, review, and validate a jsonspecs rules package."
policy:
  allow_implicit_invocation: true
